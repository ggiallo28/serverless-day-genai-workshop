python-dotenv==1.0.1
boto3>=1.43.0
awscli>=1.46.0
botocore>=1.43.0
pillow>=11.0.0
langchain-aws>=1.7.0
opensearch-py==2.7.1
langchain>=1.4.0
langchain-community>=0.4.0
langchain-classic>=1.0.0
beautifulsoup4
markdownify>=0.14.0
uv==0.9.11
bedrock-agentcore>=1.23.0
# NOTE: bedrock-agentcore-starter-toolkit (the Python `agentcore` CLI) is deprecated by AWS
# in favor of the npm-distributed `@aws/agentcore` CLI -- installing the Python package puts
# a conflicting, incompatible `agentcore` binary on PATH (different flags, no --version, no
# harness.json/add-memory/add-gateway/add-tool support). The real CLI notebooks 4-6 use is
# installed via `npm install -g @aws/agentcore` (see resources/scripts/studio_lifecycle_config.sh).
json_repair
